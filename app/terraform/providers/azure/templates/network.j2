resource "azurerm_virtual_network" "{{ network_id }}" {
  name                = "{{ network_name }}"
  address_space       = ["{{ network_cidr }}"]
  location            = azurerm_resource_group.{{ resource_group_id }}.location
  resource_group_name = azurerm_resource_group.{{ resource_group_id }}.name
}
